A simple wrapper around `opusenc` to convert an entire directory to Opus for playback from a phone.

We use an `.ogg` extension rather than `.opus` because Android supports Opus, but only with an Ogg extension.
